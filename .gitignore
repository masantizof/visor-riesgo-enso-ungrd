# Fuente cruda del índice de riesgo municipal (252 MB, resolución catastral).
# NUNCA se commitea: solo su versión simplificada en data/reference/ va al repo.
# Ver scripts/simplificar_indices_riesgo.py
/DatosIndice.geojson
/files.zip

# Emergencias Sala de Crisis: la copia canónica vive en data/reference/
# (ver sala_crisis.py); estos son el original adjunto y su respaldo crudo.
/datos_maestros_limpios.parquet
/EMERGENCIAS_SALA_CRISIS.csv

# Documentos de requerimiento/planificación (no son parte de la app).
/*.docx
/promts.zip
/PROMPT_*.md
/ref_*.png

# Grillas de modelo (WRF/GFS): pesan decenas-cientos de MB por corrida y no
# se renderizan en la app (solo botón de descarga); no se versionan a diario
# para no reventar el límite gratuito de GitHub/Streamlit Cloud.
data_lake/wrf00_netcdf/
data_lake/wrf00_tif/
data_lake/gfs06_grib2/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Streamlit
.streamlit/secrets.toml

# SO / editor
.DS_Store
Thumbs.db
.vscode/
